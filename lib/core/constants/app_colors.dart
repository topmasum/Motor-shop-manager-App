import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE53935);

  static const Color background = Color(0xFF121212); // Deep Slate
  static const Color surface = Color(0xFF1E1E1E);    // Slightly lighter dark slate for cards
  static const Color accent = Color(0xFFE53935);     // Motor Red

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFAFAFAF); // Soft Grey
  static const Color inputFill = Color(0xFF1E1E1E);
}